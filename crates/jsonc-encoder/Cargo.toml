[package]
name = "jsonc-encoder"
version = "0.1.0"
edition = "2024"

[dependencies]
jsonc-bytecode = { path = "../jsonc-bytecode" }
jsonc-mlir = { path = "../jsonc-mlir" }
flate2 = { version = "1.1.9", default-features = false, features = ["rust_backend"] }
zstd = "0.13.3"
